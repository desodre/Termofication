import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Placeholder test — integration tests require a running API server.
    expect(true, isTrue);
  });
}
